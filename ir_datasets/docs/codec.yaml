_:
  pretty_name: 'CODEC'
  desc: '
<p>
CODEC Document Ranking sub-task.
</p>
<ul>
<li>Documents: curated web articles</li>
<li>Queries: challenging, entity-focused queries</li>
<li><a href="https://github.com/grill-lab/CODEC">Task Repository</a></li>
<li>See also: <a class="ds-ref">kilt/codec</a>, the entity ranking subtask</li>
</ul>
'
  docs_instructions: &inst "docs available from dataset authors"
  data_access: '
<p>
To use this dataset, you need a copy the document corpus from <a href="https://github.com/grill-lab/CODEC">here</a>.
</p>
<p>
The process involves emailing a dataset author, who will provide instructions for downloading
the dataset.
</p>
<p>
ir_datasets expects the source file to be copied/linked under <kbd>~/.ir_datasets/codec/v1/comets_documents.jsonl</kbd>.
</p>
'
  bibtex_ids: ['mackie2022codec']

economics:
  desc: '
<p>
Subset of <a class="ds-ref">codec</a> that only contains topics about economics.
</p>
'
  docs_instructions: *inst
  bibtex_ids: ['mackie2022codec']

history:
  desc: '
<p>
Subset of <a class="ds-ref">codec</a> that only contains topics about history.
</p>
'
  docs_instructions: *inst
  bibtex_ids: ['mackie2022codec']

politics:
  desc: '
<p>
Subset of <a class="ds-ref">codec</a> that only contains topics about politics.
</p>
'
  docs_instructions: *inst
  bibtex_ids: ['mackie2022codec']
